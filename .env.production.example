# =============================================================================
# Variáveis de Ambiente para PRODUÇÃO - Render.com
# =============================================================================
# ⚠️ NÃO commitar este arquivo com valores reais!
# Este é apenas um template para configurar no dashboard do Render
# =============================================================================

# -----------------------------------------------------------------------------
# Server
# -----------------------------------------------------------------------------
NODE_ENV=production
PORT=5000

# -----------------------------------------------------------------------------
# Database (Render fornece automaticamente)
# -----------------------------------------------------------------------------
# O Render gera isso quando você cria um PostgreSQL
# Formato: postgresql://user:password@host.region.render.com:5432/database_name
DATABASE_URL=<será-fornecido-automaticamente-pelo-render>

# -----------------------------------------------------------------------------
# CORS - Segurança
# -----------------------------------------------------------------------------
# Para desenvolvimento/teste, use "*"
# Para produção, especifique seu domínio:
CORS_ORIGIN=https://seu-app.onrender.com
# Ou use "*" se quiser permitir qualquer origem

# -----------------------------------------------------------------------------
# JWT - IMPORTANTE: Gere chaves novas e fortes!
# -----------------------------------------------------------------------------
# ⚠️ NUNCA use as mesmas keys de desenvolvimento em produção!
# 
# Para gerar chaves seguras:
# Node: node -e "console.log(require('crypto').randomBytes(64).toString('hex'))"
# PowerShell: [Convert]::ToBase64String([System.Security.Cryptography.RandomNumberGenerator]::GetBytes(64))
# Online: https://generate-secret.vercel.app/64

JWT_SECRET=<gerar-chave-forte-64-caracteres>
JWT_EXPIRES_IN=7d

JWT_REFRESH_SECRET=<gerar-outra-chave-forte-diferente>
JWT_REFRESH_EXPIRES_IN=30d

# -----------------------------------------------------------------------------
# Google OAuth (Opcional)
# -----------------------------------------------------------------------------
# Configure em: https://console.cloud.google.com/
# 1. Crie projeto
# 2. Configure OAuth consent screen
# 3. Crie credenciais OAuth 2.0 Client ID
# 4. Adicione Authorized redirect URI: https://seu-app.onrender.com/api/auth/google/callback

GOOGLE_CLIENT_ID=seu-google-client-id-aqui
GOOGLE_CLIENT_SECRET=seu-google-client-secret-aqui
GOOGLE_CALLBACK_URL=https://seu-app.onrender.com/api/auth/google/callback

# -----------------------------------------------------------------------------
# Email - Recuperação de Senha (Opcional)
# -----------------------------------------------------------------------------
# Para Gmail:
# 1. Ative verificação em 2 etapas
# 2. Gere uma "Senha de app" em https://myaccount.google.com/apppasswords
# 3. Use essa senha (não a senha normal do Gmail)

EMAIL_HOST=smtp.gmail.com
EMAIL_PORT=587
EMAIL_SECURE=false
EMAIL_USER=seu-email@gmail.com
EMAIL_PASSWORD=sua-senha-de-app-aqui
EMAIL_FROM=noreply@financas360.com

# -----------------------------------------------------------------------------
# Frontend URL (será a mesma URL da aplicação)
# -----------------------------------------------------------------------------
FRONTEND_URL=https://seu-app.onrender.com

# =============================================================================
# Resumo de Configuração no Render.com
# =============================================================================
#
# 1. Crie PostgreSQL Database:
#    - Name: financas360-db
#    - Copie a DATABASE_URL fornecida
#
# 2. Crie Web Service:
#    - Environment: Docker
#    - Branch: main
#    - Dockerfile: ./Dockerfile
#
# 3. Adicione as variáveis acima em Environment Variables
#
# 4. Deploy será automático!
#
# =============================================================================
