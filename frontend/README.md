# Finanças 360° - Frontend

Interface moderna construída com React, TypeScript e Material-UI.

## 🚀 Como Rodar

### 1. Instalar Dependências
```bash
bun install
```

### 2. Configurar Variáveis
Copie o arquivo de exemplo:
```bash
cp .env.example .env
```
Certifique-se que `VITE_API_URL` aponta para seu backend (ex: `http://localhost:5000`).

### 3. Iniciar Servidor
```bash
bun run dev
```

## 🛠️ Scripts

- `bun run dev`: Inicia servidor de desenvolvimento
- `bun run build`: Gera build de produção em `./dist`
- `bun run preview`: Visualiza o build localmente
- `bun run lint`: Verifica erros de linting
